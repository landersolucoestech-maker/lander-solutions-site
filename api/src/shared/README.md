# API shared

Future backend primitives shared by multiple domain modules live here: errors, validation, authorization primitives, observability contracts and infrastructure-neutral helpers. No provider-specific implementation belongs here by default.
