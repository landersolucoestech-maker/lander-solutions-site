# API modules

Future backend domain modules live here. Keep boundaries aligned with the frontend domain model where appropriate, but do not mirror UI structure mechanically or implement nonexistent services.
