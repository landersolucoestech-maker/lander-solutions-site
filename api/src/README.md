# API source

Reserved backend source boundary. No backend runtime is implemented yet. Future code belongs under `modules/`, shared backend primitives under `shared/`, and validated runtime configuration under `config/`. Do not add fake endpoints, persistence, authentication, queues, jobs or provider connections.
