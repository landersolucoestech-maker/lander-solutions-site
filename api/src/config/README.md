# API config

Future validated backend runtime configuration lives here. Environment variables, provider configuration and secrets must be validated at runtime and must never be hardcoded or represented as connected before real credentials and providers exist.
