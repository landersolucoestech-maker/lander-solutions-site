function crmMockSeedPayouts(ctx){state.__crmParticipationService=null;const ps=crmParticipationService(),s=ValtrenPayoutCore.createState(),service=ValtrenPayoutCore.createService(s,{participationFeed:(filters={})=>ps.obligationsFeed(filters),financeService:()=>state.__crmFinanceService,partyService:state.__crmCanonicalPartyService,now:ctx.dates.now,idFactory:crmMockIds('payout'),actorProvider:()=>null});service.syncObligations();const first=service.data.obligations.find(x=>x.participationCalculationId==='mock_participation_a'),second=service.data.obligations.find(x=>x.participationCalculationId==='mock_participation_b');if(Number(first?.amountDue)!==14400||Number(second?.amountDue)!==9600)throw new Error(`Mock Repasse recebeu obrigações inválidas: A=${first?.amountDue} B=${second?.amountDue}`);if(first){const tx=state.__crmFinanceService.createTransaction({id:'mock_tx_payout_a',financialAccountId:'mock_account_bank',amount:14400,direction:'outflow',financialNature:'expense',categoryId:'commission',originalDescription:'Repasse liquidado de demonstração',transactionDate:'2026-09-10',settlementDate:'2026-09-10',status:'posted',businessDimension:'product',productId:'mock_product_alpha',counterpartyType:'person',counterpartyId:'mock_person_ana',source:'manual',isDemo:false});service.linkPayment(first.id,tx.id);}state.crmPayouts=s;state.__crmPayoutService=service;return service;}
