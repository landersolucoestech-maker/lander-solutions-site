function crmMockSeedPayouts(ctx){state.__crmParticipationService=null;const ps=crmParticipationService(),s=ValtrenPayoutCore.createState(),service=ValtrenPayoutCore.createService(s,{participationProvider:(filters={})=>ps.obligationsFeed(filters),financeService:()=>state.__crmFinanceService,partyService:state.__crmCanonicalPartyService,now:ctx.dates.now,idFactory:crmMockIds('payout'),actorProvider:()=>null});service.syncObligations();const first=service.data.obligations.find(x=>x.participationCalculationId==='mock_participation_a');if(first){const tx=state.__crmFinanceService.createTransaction({id:'mock_tx_payout_a',financialAccountId:'mock_account_bank',amount:14400,direction:'outflow',financialNature:'expense',categoryId:'commission',originalDescription:'Repasse liquidado de demonstração',transactionDate:'2026-09-10',settlementDate:'2026-09-10',status:'posted',businessDimension:'product',productId:'mock_product_alpha',counterpartyType:'person',counterpartyId:'mock_person_ana',source:'manual',isDemo:false});service.linkPayment(first.id,tx.id);}state.crmPayouts=s;state.__crmPayoutService=service;return service;}
