# Web public

Committed static assets for the frontend live here. `assets/` is the canonical public brand-asset owner. Generated bundle output is not committed here yet while the deterministic materializer remains active.
